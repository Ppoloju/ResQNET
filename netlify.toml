[build]
  base = "/"
  command = "npm run build -w shared && npm run build -w frontend"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "24"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
